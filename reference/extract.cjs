const {chromium}=require('C:/Users/ztech.pk/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('fs');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',args:['--allow-file-access-from-files']}); const page=await browser.newPage();await page.goto('file:///E:/ASTRA1/reference/video.html');await page.waitForFunction(()=>document.querySelector('video').readyState>=2);const meta=await page.evaluate(()=>{let v=document.querySelector('video');return {duration:v.duration,width:v.videoWidth,height:v.videoHeight}});console.log(meta);fs.writeFileSync('reference/meta.json',JSON.stringify(meta));for(let t=0;t<meta.duration;t+=2){let data=await page.evaluate(async t=>{let v=document.querySelector('video');v.currentTime=t;await new Promise(r=>v.addEventListener('seeked',r,{once:true}));let c=document.createElement('canvas');c.width=v.videoWidth;c.height=v.videoHeight;c.getContext('2d').drawImage(v,0,0);return c.toDataURL('image/jpeg',.88).split(',')[1]},t+.01);fs.writeFileSync(`reference/frame-${String(t).padStart(3,'0')}.jpg`,Buffer.from(data,'base64'));}await browser.close()})();
